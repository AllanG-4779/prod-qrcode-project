server:
  port: 8001
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: allang
    password: cnd80751xh
    url: jdbc:mysql://localhost:3306/qrcodedb
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  config:
    import: optional:secrets.yaml










